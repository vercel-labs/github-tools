---
"@github-tools/eve-extension": minor
"@github-tools/sdk": patch
---

Make eve-extension tools durable across multi-turn Workflow replay (inline execute + serializable tool names, fixes #51), add a `context` option to the extension config, and document that `@github-tools/sdk/connect/eve` is not durable.
