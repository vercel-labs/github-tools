---
"@github-tools/sdk": minor
---

Add `compareCommits` (ahead/behind counts, commits, and file diffs between two refs) and `getRepositoryTree` (recursive file/directory listing at a ref). `compareCommits` is available in all presets; `getRepositoryTree` is included in `repo-explorer`, `security-audit`, and `maintainer`.
