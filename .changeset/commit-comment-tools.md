---
"@github-tools/sdk": minor
---

Add five commit-comment tools: `getCommitComment`, `listCommitComments`, `createCommitComment`, `updateCommitComment`, and `deleteCommitComment`. Reads are available in the `code-review`, `repo-explorer`, and `maintainer` presets; `createCommitComment` is also in `code-review`, and the three write tools require approval by default. Also registered for the `@github-tools/sdk/eve` subpath. This brings the tool total to 47.
