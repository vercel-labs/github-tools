---
"@github-tools/sdk": minor
---

Add native eve integration via `@github-tools/sdk/eve` with `defineDynamic` tool registration, rich approval mapping (`once`, predicates), shared core refactor, and an `examples/eve-agent` starter.
